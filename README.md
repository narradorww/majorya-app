# Majorya RPG

Monorepo com o app mobile (React Native CLI) e o backend (Node + MongoDB).

## Estrutura
- `apps/MajoryaRPG` — app mobile (iOS/Android)
- `services/api` — backend (placeholder)

## Comandos rápidos
- `npm run mobile:install`
- `npm run mobile:ios`
- `npm run mobile:android`
